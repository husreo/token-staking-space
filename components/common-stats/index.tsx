import ComponentContainer from "../shared/container/ComponentContainer";
import SpacefalconStats from "./spacefalcon-stats";

const CommonStats = () => {
  return (
    <ComponentContainer className="pt-[100px] text-white">
      <SpacefalconStats />
    </ComponentContainer>
  );
};

export default CommonStats;
